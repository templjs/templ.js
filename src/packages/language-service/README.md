# @templjs/language-service

TemplJS Volar service plugin composition and schema resolution helpers.

## Boundary

- Owns language-service plugin creation and schema-source resolution used by server runtimes.
- Does not depend on VS Code extension runtime modules.
