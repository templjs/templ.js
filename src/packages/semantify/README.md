# @templjs/semantify

Semantify services for template semantic context, symbol references, and candidate planning.

## Boundary

- Owns semantic service orchestration and public semantify API contracts.
- Uses `@templjs/core` as syntax and binding authority.
- Exposes source-backed APIs consumed by language-service and editor adapters.
