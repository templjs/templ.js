<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ page.title }}</title>
</head>
<body>
    <nav>
        <ul>
        {% for item in navigation.items %}
            <li><a href="{{ item.href }}">{{ item.label }}</a></li>
        {% endfor %}
        </ul>
    </nav>

    <main>
        <h1>{{ page.title }}</h1>
        <p>{{ page.description }}</p>

        {% if page.featured_content %}
        <section class="featured">
            {{ page.featured_content | safe }}
        </section>
        {% endif %}

        <section class="content">
        {% for content in page.sections %}
            <article>
                <h2>{{ content.title }}</h2>
                <p>{{ content.body }}</p>
            </article>
        {% endfor %}
        </section>
    </main>

    <footer>
        <p>&copy; {{ current_year }} {{ company.name }}</p>
    </footer>
</body>
</html>
