---
'@templjs/volar': patch
---

# Route Alias Definitions Through Semantify References

Use semantify `resolveReferences` as the primary source for local alias definition lookups in Volar intellisense, with adapter fallback preserved and regression coverage for exact and prefix alias tokens.
