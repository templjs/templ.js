---
'@templjs/core': patch
'@templjs/language-core': patch
'@templjs/semantify': patch
'@templjs/volar': patch
---

# WI-143 Semantic Zone Vocabulary Normalization

Normalize semantic zone vocabulary to canonical `metadata` and `content` segments, and align downstream adapters/contracts to the unified zone model.
