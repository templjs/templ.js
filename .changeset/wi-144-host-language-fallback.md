---
'@templjs/core': major
'@templjs/language-core': patch
'@templjs/volar': patch
---

<!-- markdownlint-disable MD041 -->

Normalize host-language fallback terminology to use `plaintext` as the canonical value instead of `unknown` in semantic host-language resolution.

This updates core host-language contracts and aligns volar host-language zone fallback logic with the canonical fallback value.
