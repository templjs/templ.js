{
  "request_id": "{{ request.id }}",
  "timestamp": "{{ request.timestamp }}",
  "customer": {
    "id": "{{ customer.id }}",
    "tier": "{{ customer.tier }}"
  },
  "features": [
    {% for feature in features %}
    "{{ feature }}"{% if loop.last %}{% else %},{% endif %}
    {% endfor %}
  ]
}
