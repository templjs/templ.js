{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/work-management/workflows/default/status-definitions.schema.json",
  "title": "work-management/workflows/default/status-definitions",
  "description": "Repository-default work-item status, reason, category, and connectivity definitions.",
  "$comment": "EDIT THIS FILE when adding or renaming repository-default workflow vocabulary. Keep transition-profile.json mappings aligned.",
  "$defs": {
    "status": {
      "type": "string",
      "enum": ["proposed", "ready", "in-progress", "ready-for-review", "closed"]
    },
    "proposedReason": {
      "type": "string",
      "enum": [
        "needs-triage",
        "awaiting-approval",
        "deferred",
        "blocked-by-dependency",
        "blocked",
        "needs-info"
      ]
    },
    "readyReason": {
      "type": "string",
      "enum": ["prioritized", "scheduled", "dependencies-cleared", "unblocked"]
    },
    "inProgressReason": {
      "type": "string",
      "enum": [
        "implementation",
        "investigation",
        "blocked",
        "awaiting-feedback",
        "reverting-for-pr-workflow"
      ]
    },
    "readyForReviewReason": {
      "type": "string",
      "enum": [
        "awaiting-review",
        "changes-requested",
        "verification-pending",
        "implementation-complete"
      ]
    },
    "closedReason": {
      "type": "string",
      "enum": [
        "completed",
        "rejected",
        "duplicate",
        "obsolete",
        "cancelled",
        "merged_to_main",
        "merged-to-main",
        "success",
        "wontfix"
      ]
    },
    "reasonCategory": {
      "type": "string",
      "enum": ["intake", "planning", "execution", "review", "completed", "decision"]
    },
    "connectivityClass": {
      "type": "string",
      "enum": ["start", "intermediate", "end"]
    },
    "statusReasonExtensionToken": {
      "$ref": "https://raw.githubusercontent.com/templjs/templ.js/main/schemas/frontmatter/support/base/current.json#/$defs/statusReasonExtensionToken"
    }
  }
}
